name: Workforce CI Smoke
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  smoke:
    runs-on: [self-hosted, Linux, X64, ovh-workforce]
    steps:
      - run: echo SMOKE_OK && uname -a
